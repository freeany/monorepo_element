import rr{{properCase name}} from './src/{{name}}.vue'

rr{{properCase name}}.install = Vue => {
  Vue.component(rr{{properCase name}}.name, rr{{properCase name}})
}

export default rr{{properCase name}}
